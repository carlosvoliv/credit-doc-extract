import { createApp } from 'vue'
import App from './App.vue'
import 'facet-ui/styles'
import 'facet-ui/base' // paints the page canvas from theme tokens
import './app.css'

createApp(App).mount('#app')
